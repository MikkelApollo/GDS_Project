To get the clean data, add the file '995,000_rows.csv' to this folder, open 'cleaning.ipynb', uncomment the final three lines of code, and run the notebook. You may also wish to uncomment the two penultimate code cells or simply skip them to save a few minutes of runtime.

The trained models from 'simple.py' and 'advanced.py' are stored in 'simple.joblib' and 'advanced.joblib' respectively. Alternatively, 